import { InputFieldProps } from './types';
import * as React from 'react';
export declare const Field: ({ id, form, icon, label, description, iconDirection, classNameContainer, validateInputIconClassNames, isLoading, rest }: InputFieldProps) => React.JSX.Element;
