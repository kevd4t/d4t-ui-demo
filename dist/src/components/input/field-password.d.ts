import { InputFieldProps } from './types';
import * as React from 'react';
export declare const FieldPassword: ({ id, form, label, description, iconDirection, classNameContainer, isLoading, validateInputIconClassNames, rest }: InputFieldProps) => React.JSX.Element;
