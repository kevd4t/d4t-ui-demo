/// <reference types="react" />
import { ComboxCheckBoxProps } from './types';
export declare const ComboxCheckbox: ({ id, description, icon, placeholder, label, tabIndex, classNameContainer, readOnly, classNamePopover, ...rest }: ComboxCheckBoxProps) => JSX.Element;
