/// <reference types="react" />
import type { IUploadImageProps } from '../types';
export declare const UploadImage: ({ initialPreview, setUploadImage, format, label, uploadLabel, tabIndexs, disabled, emptyClassName, imageContainerClassName, zoom, compress, download, onRemove, onEdit }: IUploadImageProps) => JSX.Element;
