/// <reference types="react" />
import type { IUploadImageProps } from '../types';
export declare const UploadImage: ({ setUploadImage, format, label, uploadLabel, tabIndexs, emptyClassName, imageContainerClassName, icons, zoom, compress }: IUploadImageProps) => JSX.Element;
