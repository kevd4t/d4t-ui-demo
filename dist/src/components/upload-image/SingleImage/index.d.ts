/// <reference types="react" />
import type { IUploadImageProps } from '../types';
export declare const UploadImage: ({ initialPreview, setUploadImage, format, label, uploadLabel, tabIndexs, emptyClassName, imageContainerClassName, zoom, compress }: IUploadImageProps) => JSX.Element;
